<div class='container'>
  <div class="row">
        <div class="col-md-4">
          <%= image_tag(@item.image, size: '300x250') %>
        </div>
        <div class="col-md-4">
          <strong><%= @item.name %></strong>
          <br>
          <%= @item.introduction %>
          <br>
          ￥<%= @item.price %>
        </div>
  </div>
</div>